# 安全策略

## 支持范围

仅支持最新正式发布版本。由于 Clash for Windows 已停止维护，本项目无法承诺修复其 Electron 运行环境或第三方依赖继承的所有漏洞。

## 安全问题报告

疑似安全问题请优先使用私密渠道报告，并提供：

- 受影响版本；
- Windows 版本；
- 最小复现步骤；
- 已脱敏日志；
- 预期影响。

不要提交订阅 URL、API Token、节点密码、私钥、Cookie、服务令牌或完整用户配置。

## 信任模型

本项目包含需要管理员权限的安装、服务修复、TUN/TAP 和 ProgramData 目录权限设置操作。用户应在运行前阅读源码、校验 SHA-256，并确认下载来源可信。

## 发布校验

正式发布包应提供 SHA-256 文件。下载后建议用户自行校验压缩包哈希，确认与 Release 页面一致。
